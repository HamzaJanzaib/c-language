// hello.c
#include <stdio.h>

/*
 * hello.c
 * This is a simple C program that prints "hamza" to the console.
 * It includes the standard input/output library to use the printf function.
 */
int main(void){
    int a = 5; // This variable is declared but not used in the program.
    printf("hamza \n"); // This line prints "hamza" followed by a newline character to the console.
    printf("%d \n", a); // This line prints the value of variable 'a' followed by a newline character to the console.
    return 0;
}
